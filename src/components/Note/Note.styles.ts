import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    noteContainer: {
        backgroundColor: '#FDE047',
        padding: 20,
        marginVertical: 8,
        borderRadius: 10,
        width: '100%',
    },
    noteTitle: {
        fontSize: 18,
        fontWeight: 'bold',
        color: '#1F2937',
    },
});
