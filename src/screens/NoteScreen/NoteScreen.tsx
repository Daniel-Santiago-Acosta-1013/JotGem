import React from 'react';
import { View, Text } from 'react-native';

const NoteScreen: React.FC = () => {
    return (
        <View>
            <Text>Este es el componente NoteScreen</Text>
        </View>
    );
};

export default NoteScreen;